Guardar cambio y subir
	git add .
	git commit -m "3do"
	git push		primera vez poner git push -u repositorio

	git commit -am "aa"
	git push

Recibir cambio
	git pull

git log
git status

Setear variable locales MAC
git config --global user.name "Alfredo Bordagorry"
git config --global user.email abordagorry@gmail.com


//SUBIO TODO
echo "# sss" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/abordagorry/sss.git
git push -u origin master


git remote add origin https://github.com/abordagorry/sss.git
git push -u origin master